events {}

http {
    server {
        # Nginx будет слушать стандартный веб-порт 80
        listen 80;

        location / {
            # Перенаправляем все запросы на сервис my-web-app на порт 8080
            proxy_pass http://my-web-app:8080/;
        }
    }
}